import { Grid, Typography } from "@mui/material";

export default function RegisUser() {
    return (
        <Grid display={'flex'} justifyContent={'center'}>
            <Typography variant="h3" marginTop={'40px'}>Create Account</Typography>
        </Grid>
    )
}